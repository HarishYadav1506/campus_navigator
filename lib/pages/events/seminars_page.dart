import 'package:flutter/material.dart';

class SeminarsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Seminars')),
      body: Center(child: Text('Seminars Page')),
    );
  }
}
