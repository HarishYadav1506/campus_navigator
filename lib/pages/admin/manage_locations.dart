import 'package:flutter/material.dart';

class ManageLocations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Locations')),
      body: Center(child: Text('Manage Locations')),
    );
  }
}
