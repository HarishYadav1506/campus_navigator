import 'package:flutter/material.dart';

class IpBtpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('IP/BTP')),
      body: Center(child: Text('IP/BTP Page')),
    );
  }
}
