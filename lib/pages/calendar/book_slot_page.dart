import 'package:flutter/material.dart';

class BookSlotPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Book Slot')),
      body: Center(child: Text('Book Slot Page')),
    );
  }
}
